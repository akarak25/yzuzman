/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router ile i18n yapılandırması
  // i18n artık yapılandırma dosyasından çıkarıldı
  // dinamik route segment ([locale]) ile yönetiliyor
}

module.exports = nextConfig